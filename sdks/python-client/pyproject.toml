[build-system]
# Cap below hatchling 1.32.0, which flipped the default core-metadata-version
# to 2.5 (2026-08-11) — ahead of what twine and PyPI's Warehouse accept, so
# `twine`/upload rejects the wheel with "'2.5' is not a valid metadata version".
# 1.27..<1.32.0 emits Metadata-Version 2.4. Drop the cap (or set an explicit
# `core-metadata-version = "2.4"` on the build targets) once 2.5 is accepted.
requires = ["hatchling>=1.27,<1.32.0"]
build-backend = "hatchling.build"

[project]
name = "omnigent-client"
version = "0.16.0.dev0"
description = "Python client SDK for the omnigent server API"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.12"
dependencies = [
    # Sibling package — provides the SessionStreamEventType /
    # StreamEvent source of truth that the SDK validates SSE
    # envelopes against (see _sessions.py, _sse.py). Resolved via
    # the relative path below so the SDK can be installed
    # editable alongside the root ``omnigent`` package. Version-locked
    # (==) so a published SDK always pairs with the server release it
    # shipped with (release-omnigent.yml verifies the pin).
    "omnigent==0.16.0.dev0",
    "httpx>=0.27",
    "pydantic>=2.0,<3",
]

[tool.uv.sources]
omnigent = { path = "../..", editable = true }
